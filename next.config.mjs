/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "potenzaeducare.com",
      "firebasestorage.googleapis.com",
      "img.clerk.com",
    ],
  },
};

export default nextConfig;
